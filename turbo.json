{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"],
      "env": [
        "NODE_ENV",
        "NEXT_PUBLIC_API_URL",
        "PGHOST",
        "PGUSER",
        "PGPASSWORD",
        "PGDATABASE"
      ]
    },
    "lint": {},
    "dev": {},
    "test": {},
    "check:lint": {},
    "check:types": {},
    "check:styles": {}
  }
}
